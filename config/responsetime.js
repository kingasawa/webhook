module.exports.responsetime = {
  header: 'X-Response-Time', // Header string used to return with the response time
  template: "%d", // Template to be used for the X-Response-Time
  decimals: 0 // Number of decimals to return
};
